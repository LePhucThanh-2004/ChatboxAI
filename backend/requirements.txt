fastapi              # Framework API
uvicorn              # Server ASGI để chạy FastAPI
sqlmodel             # ORM cho PostgreSQL (sẽ thay thế psycopg2 nếu cần)
apscheduler          # Lập lịch tác vụ (có thể dùng cho events)
dateparser           # Phân tích chuỗi ngày giờ (hữu ích cho events)
google-auth          # Xác thực Google (có thể dùng cho Gemini API nếu cần)
nltk==3.9.1          # Xử lý ngôn ngữ tự nhiên (có thể dùng để tiền xử lý văn bản)
python-dotenv==1.0.1 # Quản lý biến môi trường (chỉ cần một phiên bản)
qdrant_client        # Client cho Qdrant Vector DB
sentence_transformers # Tạo embedding văn bản
python-docx          # Đọc file .docx
requests             # Gửi yêu cầu HTTP (dùng cho Gemini API)
PyPDF2               # Đọc file .pdf
psycopg2-binary      # Kết nối PostgreSQL (có thể thay bằng sqlmodel)
python-multipart     # Xử lý upload file multipart (cần cho FastAPI)